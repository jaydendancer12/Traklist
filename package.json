{
  "name": "traklist",
  "version": "1.0.0",
  "description": "Scan. Search. Queue. Let the crowd control the music.",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
